#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	install -Dm755 bin/proxmox-storage-migrate debian/proxmox-storage-migrate/usr/bin/proxmox-storage-migrate
	install -Dm644 config/proxmox-storage-migrate.default debian/proxmox-storage-migrate/etc/default/proxmox-storage-migrate
